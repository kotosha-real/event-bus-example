/**
 * Here we introduce EventBus. Yes, it's that simple
 * Main idea is that we will listen and fire events on this Vue instance
 * Dive into App.vue and Child.vue to find implementation details
 */

import Vue from 'vue'

export default new Vue()
